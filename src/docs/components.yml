components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
          format: email
        name:
          type: string
        role:
          type: string
          enum: [user, admin]
      example:
        id: 5ebac534954b54139806c112
        email: fake@example.com
        name: fake name
        role: user

    Device:
      type: object
      properties:
        id:
          type: string
        modalName:
          type: String
        srNo:
          type: String
        uuid:
          type: String
        variant:
          type: String
        category:
          type: String
        manufacturer:
          type: String
        picture:
          type: String
        isIssued:
          type: Boolean
      example:
        id: 5ebac534954b54139806c112
        modalName: iPhone 12,
        srNo: 123dadda1,
        uuid: asdasd231323,
        variant: 128GB,
        category: Mobile,
        manufacturer: Apple,
        picture: http://placeimg.com/640/480,
        isIssued: true

    DeviceTransactions:
      type: object
      properties:
        id:
          type: string
        deviceId:
          type: String
        userId:
          type: String
        dueDate:
          type: String
        issuedOn:
          type: String
        submittedOn:
          type: String
      example:
        id: 5ebac534954b54139806c423
        deviceId: 5fdcbc30a0e5c50e540016cd,
        userId: 5fc2790af6a6bbc45afba0d6,
        dueDate: 2021-06-15T15:06:54.348Z,
        issuedOn: 2021-06-05T15:16:54.348Z,
        submittedOn: 2021-15-05T15:05:54.348Z,

    Token:
      type: object
      properties:
        token:
          type: string
        expires:
          type: string
          format: date-time
      example:
        token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI1ZWJhYzUzNDk1NGI1NDEzOTgwNmMxMTIiLCJpYXQiOjE1ODkyOTg0ODQsImV4cCI6MTU4OTMwMDI4NH0.m1U63blB0MLej_WfB7yC2FTMnCziif9X8yzwDEfJXAg
        expires: 2020-05-12T16:18:04.793Z

    AuthTokens:
      type: object
      properties:
        access:
          $ref: '#/components/schemas/Token'
        refresh:
          $ref: '#/components/schemas/Token'

    Error:
      type: object
      properties:
        code:
          type: number
        message:
          type: string

  responses:
    DuplicateEmail:
      description: Email already taken
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: 400
            message: Email already taken
    DuplicateSrNo:
      description: Serial number already taken
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: 400
            message: Serial number already taken
    DuplicateUUID:
      description: UUID already taken
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: 400
            message: UUID already taken
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: 401
            message: Please authenticate
    Forbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: 403
            message: Forbidden
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: 404
            message: Not found
    DeviceAlreadyBooked:
      description: Device already booked and in-use
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            code: 400
            message: This device already booked
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
